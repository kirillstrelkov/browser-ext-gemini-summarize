{
  "name": "ff-ext-gemini-summarize",
  "version": "1.1.0",
  "description": "This project is a Browser extension that uses Gemini API to summarize text.",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist && rm -rf node_modules",
    "fmt": "npx prettier --write .",
    "lint": "npx eslint .",
    "zip": "mkdir -p dist && zip -r dist/ff-ext-gemini-summarize.zip icons manifest.json src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^9.32.0",
    "@eslint/json": "^0.13.1",
    "@eslint/markdown": "^7.1.0",
    "eslint": "^9.32.0",
    "globals": "^16.3.0",
    "inquirer": "^12.9.0",
    "prettier": "3.6.2"
  }
}
